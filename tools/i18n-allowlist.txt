# i18n hard-gate allowlist
#
# One entry per line.
# - Exact literal match: copied as-is
# - Regex match: prefix with `re:`
#
# Keep this list tight. Every entry should be reviewable and justified.

MB
kWh
kW
°C
URL
HTTP
HTTPS
API
JSON
CSV
UUID
MQTT
KNX
# Dynamic :title bindings — computed expressions, not string literals
re:^nodeFullPath\(
re:^ownerTitle\(
re:^f\.label
# Technical placeholders / examples in NodeConfigPanel (formulas, JSON examples, external URLs)
a + b
crontab.guru ↗
re:^x \* 100$
re:^\{"X-Api-Key"

# GenericNode.vue — NODE_DEFS static node-type labels (first label: per entry).
# Port labels are now fully translated via t(); the node-type labels below are
# either single-word technical tokens (auto-passed) or require an allowlist entry.
re:^(Max|Min)∞$
re:^(JSON|XML) Extraktor$
re:^SMS \(seven\.io\)$
re:^∅
Verzögerung
MCP Tool
Astro Sonne
Tagsüber
Temp °C
Zähler
String Verketten
API Client
Substring / RegEx
Wake on LAN

# SchemaForm.vue — defaultPlaceholder() returns a schema default value (e.g. "502", "3.0")
# from the backend JSON schema, not a hardcoded user-facing string.
re:^defaultPlaceholder\(
